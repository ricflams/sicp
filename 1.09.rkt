#lang sicp
(#%require "util.rkt")

;; exercise 1.9
(display "exercise 1.9\n")


;; (define (+ a b)
;;   (if (= a 0)
;;       b
;;       (inc (+ (dec a) b))))

(display "is recursive\n")

;; (define (+ a b)
;;   (if (= a 0)
;;       b
;;       (+ (dec a) (inc b))))

(display "is iterative\n")

