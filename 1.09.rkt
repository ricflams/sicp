#lang sicp

;; exercise 1.9
(display "exercise 1.9")
(newline)

;; (define (+ a b)
;;   (if (= a 0)
;;       b
;;       (inc (+ (dec a) b))))

(display "is recursive")
(newline)

;; (define (+ a b)
;;   (if (= a 0)
;;       b
;;       (+ (dec a) (inc b))))

(display "is iterative")
(newline)

